

const CountryData = ({country,handleVisited,handleVisitedFlags}) => {
    return (
        <div>
            
             <h4>Country Data: {country.name.common}</h4>

        </div>
    );
};

export default CountryData;